import { ValidationLabelDirective } from './validation-label.directive';

describe('ValidationLabelDirective', () => {
  it('should create an instance', () => {
    /*const directive = new ValidationLabelDirective();
    expect(directive).toBeTruthy();*/
  });
});
