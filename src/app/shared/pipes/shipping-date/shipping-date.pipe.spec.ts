import { ShippingDatePipe } from './shipping-date.pipe';

describe('ShippingDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ShippingDatePipe();
    expect(pipe).toBeTruthy();
  });
});
