import { IConfig } from './config.interface';

export const config: IConfig = {
  country: 'PE',
  localeId: 'PEN',
  currencyCode: 'PEN',
  documentId: {
    name: 'DNI',
    documentLength: 10,
    sample: '12345678-9',
  },
};
